=== Task 7: Config Fixes ===
Date: 2026-04-05T18:07:12Z

--- mypy config (after) ---
[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = false
ignore_missing_imports = true


--- ruff config (after) ---
[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "A", "C4", "T20", "RUF"]
ignore = [
    "SIM115",  # open() without context manager — intentional in file-upload patterns
    "A003",    # class attribute 'type' shadows builtin — matches API field names
]

--- ruff check (runs without crash) ---

Found 1 error.
No fixes available (1 hidden fix can be enabled with the `--unsafe-fixes` option).

--- mypy verify ---
Success: no issues found in 1 source file

RESULT: Both tools accept new config. Violations expected (fixed in other tasks).
