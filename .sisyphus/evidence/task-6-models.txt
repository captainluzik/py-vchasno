Task 6: Models & Enums Fixes — Evidence
========================================
Date: 2026-04-05

## Changes Made

### 1. extra="allow" added to ALL API response models (40 models, 7 files)

documents.py (17 models):
  - Signature, SignatureDetail, Recipient, Version, FlowEntry
  - Review, ReviewRequest, ReviewStatus, Comment, CommentList
  - DocumentStatusItem, DocumentStatusList, DownloadDocument, DownloadDocumentList
  - _BaseDocument (covers Document + IncomingDocument via inheritance)
  - DocumentList, IncomingDocumentList

common.py (8 models):
  - DocumentCategoryInfo, CustomField, DocumentField
  - ReportRequest, ReportStatus, CompanyCheck, CompanyCheckUpload, UpdatedIds

archive.py (4 models):
  - ArchiveDirectory, ArchiveDirectoryList, ArchiveImportSignedResult, ArchiveScanResult

cloud_signer.py (5 models):
  - CloudSignerSession (merged with existing populate_by_name)
  - CloudSignerSessionCheck, CloudSignerRefreshCheck (merged), CloudSignerRefresh (merged)
  - SignSession

groups.py (2 models): Group, GroupMember
roles.py (2 models): Role, RoleList
tags.py (4 models): Tag, TagList, TagRole, TagRoleList

### 2. AuthenticationError docstring
Changed: "403 - authentication / authorization failure."
To:      "401/403 — authentication or authorization failure."

### 3. JSON parsing robustness in _async/endpoints/_base.py
- Changed content-type check: startswith("application/json") → "json" in content_type
- Empty body → returns None (no crash)
- Malformed JSON → raises VchasnoError (not JSONDecodeError)

### 4. from __future__ import annotations in __init__.py
Already present (line 3) — no change needed.

## Verification Output

$ python3 -c "..."
extra=allow: ALL 40 models OK
AuthenticationError docstring: OK
__init__.py annotations: OK
ALL CHECKS PASSED

## LSP Diagnostics
All 9 changed files: 0 errors

## Files NOT modified (as required)
- _sync/ directory: untouched
